# TODO list
    [ ] Update company branch
    [ ] Update Product
    [ ] Update Customer
    [ ] Change all ' = ""' by string.Empty
    [ ] Remove command validator for company branch list (it's empty)
    [ ] Remove command validator for customer list (it's empty)
    [ ] Remove command validator for product list (it's empty)


The project was done within 3 days
